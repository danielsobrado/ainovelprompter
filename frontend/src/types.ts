// types.ts

export interface TaskTypeOption {
    id: string;
    label: string;
    description: string;
}
  
export interface RulesOption {
    id: string;
    label: string;
    description: string;
}
  
// Option interfaces

export interface SampleChapterOption {
    id: string;
    label: string;
    content: string;
  }
  
  export interface TaskTypeOption {
    id: string;
    label: string;
    description: string;
  }
  
  export interface RuleOption {
    id: string;
    label: string;
    description: string;
  }
  
  export interface CharacterOption {
    id: string;
    label: string;
    description: string;
  }
  
  export interface LocationOption {
    id: string;
    label: string;
    description: string;
  }
  
  export interface CodexOption {
    id: string;
    label: string;
    description: string;
  }
  